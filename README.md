# Проверка контрагентов

Помощник для проверки контрагентов под конкретную задачу: найти компании,
уточнить условия сделки, сопоставить существенные сведения и определить следующий шаг.
Исходная постановка и Q&A — в [индексе материалов](artifacts/INDEX.md);
командные продуктовые документы — в [docs](docs/README.md).

## Запуск

```bash
uv sync
uv run python src/main.py
```

Открыть [localhost:8000](http://127.0.0.1:8000). Остановить сервер — `Ctrl+C`.
Если порт занят: `COUNTERPARTY_PORT=8011 uv run python src/main.py`.
Активировать `.venv` для команд `uv` не нужно.

При первом запуске скопируйте `.env.example` в `.env` и укажите
`COUNTERPARTY_SNAPSHOT_JSON_PATH`. Ключ модели задаётся только через
`COUNTERPARTY_LLM_API_KEY`. Текущее локальное подключение сохранено;
его адрес и модель настраиваются через `COUNTERPARTY_LLM_BASE_URL` и
`COUNTERPARTY_LLM_MODEL`. Используется совместимый Chat Completions API.
Карточка и сравнение по явным реквизитам работают без ключа;
понимание свободного текста и AI-диалог требуют подключения.
`/api/health` показывает доступность данных и AI, но не секреты.

Собранный интерфейс включён в репозиторий: для обычного запуска Node не нужен.
После изменения frontend:

```bash
cd frontend
pnpm install --frozen-lockfile
pnpm build
pnpm test
```

Для разработки нужен Node 22+ и pnpm. Vite собирает UI в
`src/counterparty_agent/ui/build`; FastAPI раздаёт его тем же процессом.

## Как пользоваться

1. Ввести ИНН, ОГРН, название или свободную просьбу найти/сравнить компании.
   Несколько явных реквизитов можно разделить `;`.
2. При неоднозначности подтвердить компанию по реквизитам.
3. Сообщить помощнику цель и известные условия. Они могут быть в первом запросе;
   повторно сообщать их не нужно. Можно выбрать «Общая проверка».
4. Получить краткий анализ, основания, ограничения и следующие действия.
5. Изменить условия, например аванс на оплату после поставки, и уточнить вывод.
6. При необходимости раскрыть полный отчёт/сравнение; нажать показатель для источника.
7. Необязательно сохранить проект: добавить договор, ответить на открытый вопрос,
   выполнить проверку и просмотреть предложенные изменения резюме.
8. Явно подтвердить резюме. Автоодобрения сделки нет.

Карточка открывается с краткой сводки: реквизиты, оценка банка, финансовые показатели,
записи для внимания и ограничения данных. Полный текст доступен в раскрывающихся
разделах; кнопка «Раскрыть все» показывает весь отчёт. Счётчики обозначают записи,
а не независимые риски. Банковский светофор не объясняется этими записями.
В разделе «Финансы» доступны виджеты и линейный график годовых значений с выбором
показателя и переключателем «Линия / Столбцы». Пропуски остаются разрывами.
Сравнение также начинается с краткой матрицы: статус, выручка, прибыль, сигналы
внимания и ограничения. Полные показатели раскрываются по темам. Раздел «Группа
в цифрах» показывает распределение банковских оценок и наличие финансовых данных
по всей группе. Нажатие на значение или элемент сводки открывает источники.
Фильтры и страницы применяются к обеим матрицам, не меняя исходные номера компаний;
серый, отсутствующий и неизвестный банковский сигнал фильтруются отдельно.
Графики не назначают валюту неизвестным суммам и не создают финансовый рейтинг.

Примеры команд: «Сравни ИНН …; ИНН …», «Подробнее про вторую»,
«Добавь к сравнению ИНН …», «Покажи сравнение», «А почему 1 требует внимания?».
Вопрос по номеру относится к исходному порядку сравнения. В ответе на вопрос о
причинах цвета помощник разделяет банковскую оценку и отдельные сигналы по данным
этой компании: сигналы не объявляются причинами закрытого банковского скоринга.
В проектном чате конкретную компанию надёжнее указывать по ИНН;
порядковые ссылки используйте в чате таблицы.

В чате проверки свободный текст сначала разбирает LLM-роутер: поиск, сравнение,
добавление в группу, вопрос по отчёту или уточнение. Например, «Из-за чего этот
контрагент надёжен?» и «А каккие есть судебные дела?» относятся к текущей карточке.
Код проверяет извлечённые реквизиты и исполняет разрешённое действие. Узкий вопрос
использует выбор готовых фактов; аналитический — цикл проверки и связный ответ.
Точный ИНН, явный список реквизитов, кнопки и восстановление страницы обходят роутер.
Анализ после выбора может вызвать модель, если цель уже известна. Ошибка роутера сохраняет выбор и
показывает причину отказа; целый непонятый вопрос не используется как имя компании.
Без AI остаётся ограниченный ручной режим явных команд, не полноценный разбор речи.

Лимит десяти участников снят. Проверены группы до 100 компаний.
Таблица показывает до шести колонок на странице, сохраняя весь состав.
Это не обещание неограниченной производительности: запрос ограничен 12 000 символами,
данные — подключённой выборкой. Финансового рейтинга и автоматического победителя нет.

## Три уровня решения

| Уровень | Что выполняет код | Где используется AI |
|---|---|---|
| Отчёт и сравнение | Поиск, расчёты, периоды, пропуски, таблица | Только понимание свободного запроса |
| Диалог | Адресат, расчёты, источники, числа и границы сессии | Намерение, условия, связный анализ |
| Сохранённый проект | Чтение разделов/документов, версии и подтверждение | Выбор следующего шага и синтез отчётов с документом |

Узкие вопросы о показателях сохраняют режим выбора готовых фактов. Аналитические
вопросы проходят отдельный ограниченный LangGraph: выбрать раздел → прочитать →
выбрать следующий шаг, уточнить или завершить. До четырёх решений о следующем шаге,
до двух уточнений за проверку. Цель и условия хранятся отдельно от отчёта как
сведения пользователя. Изменённое условие заменяет прежнее в текущем контексте.

LLM формулирует связные блоки «вывод / факт / что неизвестно / следующий шаг».
Сервер проверяет ID, принадлежность источников и числа; отдельный вызов модели
проверяет обоснованность и соответствие вопросу. Это снижает ошибки, но не является
доказательством истинности интерпретации: экспертная оценка остаётся обязательной.
Документы — недоверенные данные, инструкции внутри них не выполняются.

`zskRiskLevel` — неизменяемый внешний банковский сигнал: зелёный — надёжный,
жёлтый — требует внимания, красный — в зоне риска, серый — нет данных для оценки.
Он не пересчитывается и не объясняется собственными факторами приложения.
Отсутствующий сигнал сохраняется отдельно от явно переданного `GREY`.
Отсутствие данных не означает отсутствие риска.

## Карта репозитория

```text
src/main.py                       единый запуск
src/counterparty_agent/
  config.py                       настройки из окружения
  models.py                       общие контракты отчёта и evidence
  query.py                        извлечение реквизитов, exact/fuzzy-поиск
  data/                           чтение JSON, нормализация, индексы, provenance
  analytics/                      правила по разделам и сравнение
  ai/                             намерение, условия пользователя, каталоги, синтез и проверка
  workflow/                       поиск и память, адаптивный цикл аналитической проверки
  api/                            HTTP, проекции, жизненный цикл и владение
  projects/                       проекты, план, документы, чат и резюме
  ui/build/                       готовая сборка интерфейса
frontend/src/
  workspace/                      оболочка, состояние и запросы
  comparison/                     таблица, карточка, форматирование
  projects/                       интерфейс сохранённого проекта
  components/                     чат, источники и общие элементы
  styles/                         тематические стили
tests/                            Python-регрессии и интеграционные сценарии
benchmarks/                       регрессии и явный живой прогон текущей модели
docs/                             архитектура и фактический статус
data/                             локальные отчёты; не коммитятся
```

Импорты ведут прямо к реализации: источник — `data/repository.py`, анализ —
`analytics/core.py`, сравнение — `analytics/comparison.py`, граф —
`workflow/builder.py`, HTTP и запуск — `api/routes.py`, LLM-транспорт —
`ai/transport.py`. `ai/router.py` проверяет JSON намерения, `workflow/semantic.py`
исполняет его в границах сессии. Отдельных файлов-переэкспортов нет;
`__init__.py` обозначают пакеты.

Стек: Python 3.12, uv, FastAPI, Pydantic, LangGraph, SQLite для памяти,
RapidFuzz, Decimal; React, TypeScript, Vite, Recharts, базовые Alfa Core Components.
SQLite хранит сессии и проекты, **не базу карточек контрагентов**.
Данные по-прежнему читаются из JSON; CSV остаётся резервным направлением,
отдельного CSV-адаптера в рабочем runtime нет. БД отчётов и MCP не добавлены.

## Ограничения и безопасность

- Источник — локальный snapshot, не оперативная банковская интеграция.
- Подбор похожих компаний и финансовое ранжирование не реализованы.
- Новая проверка не наследует диалог. Сохранённый проект открывается явно.
- В checkpoint нет полных отчётов, пользовательских вопросов и текстов ответов.
  Цель и дословные условия сохраняются в отдельном поле текущей SQLite-сессии,
  удаляются вместе с ней и не наследуются новым пользователем или новой проверкой.
- Проекты, извлечённые тексты документов и принятые резюме сохраняются локально.
  Не используйте прототип как публичный сервис: cookie-владение не заменяет авторизацию.
- TXT/MD/PDF/DOCX: до 2 МБ, до пяти документов, 60 000 символов в документе;
  PDF до 30 страниц, без пароля. OCR отсутствует. Парсер изолирован процессом
  с лимитами времени и памяти; целевые ОС — macOS/Linux.
- В проектном анализе отбираются релевантные фрагменты документов по вопросу и цели,
  вместе с фактами отчётов и условиями пользователя. Полнота прочтения не обещается;
  ограничение выборки указывается явно. Привязка документа не закрывает вопрос сама.
- Изменение цели, состава или документов отменяет неподтверждённое предложение.
  Принятое резюме сохраняется как предыдущая версия с отметкой об изменении контекста.
- Запрос и разрешённые факты/цитаты передаются настроенному LLM-провайдеру.
  Не отправляйте через публичный прототип сведения без нужного разрешения.
- Секреты, исходные snapshots, PII и тела запросов не пишутся в Git/логи.

## Проверки и следующие задачи

```bash
uv run ruff check .
uv run mypy
uv run pytest
uv run pytest tests/test_synthetic_benchmark.py tests/test_projects.py tests/test_project_dialogue.py
```

Синтетика используется только в тестовых временных каталогах, не вместо реальных
данных приложения. Подробнее: [бенчмарк](benchmarks/README.md),
[архитектура](docs/architecture.md), [статус](docs/solution_status.md).

Платный прогон реальной подключённой модели запускается только явно:

```bash
uv run python -m benchmarks.evaluate_review --live --output /tmp/counterparty-eval.json
```

Он использует реальные отчёты из текущего JSON и временную SQLite, не меняет рабочие
сессии. Условия сделки и неподписанный проект условий — демонстрационные сведения
пользователя, не новые факты о компаниях. Результат содержит ответы для ручной оценки;
храните его локально, не коммитьте. Флаг `--suite core` ограничивает прогон основным чатом;
`--inn` можно повторить для каждого участника. Сценарий защиты — в [демо](docs/DEMO.md).

Аналитический цикл по умолчанию ограничен 120 секундами и 2400 токенами на ответ модели.
Настройки `COUNTERPARTY_LLM_REVIEW_TIMEOUT_SECONDS` и `COUNTERPARTY_LLM_REVIEW_MAX_TOKENS`
не меняют бюджет роутера. При сбое источники и условия остаются доступны.

TODO команды: экспертная оценка релевантности интерпретаций, пользовательский замер
«PDF против workspace», уточнение единиц финансов и семантики банковских полей,
демонстрационный сценарий. Для публичного запуска отдельно нужны авторизация,
защита и управление сроками хранения документов, квоты и нагрузочные проверки.
