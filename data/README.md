# Данные

Папка для локальных snapshots и обезличенных fixtures.

TODO:

- основной demo-source — вложенный `snapshot.json`;
- CSV используется как fallback и для проверки совместимости;
- raw-файлы, SQLite сессий и PII не коммитятся;
- для тестов подготовить небольшой синтетический fixture без реальных ФИО,
  телефонов, адресов и идентификаторов физических лиц.

