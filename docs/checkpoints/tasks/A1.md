# A1 — Python foundation and contract v0

- Status: implementation
- Branch: agent/a1-python-foundation
- Worktree: .worktrees/a1-python-foundation
- Owner scope: `packages/**`, `docs/checkpoints/tasks/A1.md`
- Depends on: baseline `7e9fad8`
- Last commit: `9bb4c1d`

## Готово

- Сверены архитектурные границы shared-пакетов и состав contract v0.
- Созданы самостоятельные installable `uv`-проекты contracts/domain/storage в `src` layout.
- Для каждого проекта создан собственный lockfile и import smoke test.
- Реализован contract v0: typed IDs, availability/outcome, проверяемые evidence refs и project/thread/run envelopes.

## Текущее состояние

- Skeleton-пакеты импортируются без runtime setup и I/O.
- Evidence refs не зависят от Mongo wrapper shape: сохраняют server ID и логический JSON Pointer.

## Проверки

- `git status --short` — passed: исходный task-worktree чист.
- Локальные `ruff check`, `ruff format --check`, `mypy`, `pytest` — passed после форматирования; финальный повтор требуется после contract v0.
- `packages/contracts`: `ruff check`, `ruff format --check`, `mypy`, `pytest` (11 tests) — passed.

## Решения и допущения

- Каждый shared-пакет будет самостоятельным Python 3.13 `uv`-проектом с `src` layout и без import-time I/O.
- Contract v0 покрывает типизированные IDs, evidence/availability/outcome и минимальные project/thread/run DTO; полный report/REST contract остаётся для C-01/C-02.
- `unavailable` является состоянием источника/transport, а не `FactValue.availability`; zero — available value, empty — `present_empty`, missing — `missing`.

## Следующее действие

Зафиксировать contract v0, затем повторить полный набор проверок всех трёх пакетов.
