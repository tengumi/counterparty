# A2 — web foundation

- Status: verification
- Branch: agent/a2-web-foundation
- Worktree: .worktrees/a2-web-foundation
- Owner scope: apps/web/**, docs/checkpoints/tasks/A2.md
- Depends on: baseline  (first-wave foundation)
- Last commit: 0774222 (discovery)

## Готово

- Прочитаны SUBAGENT_GUIDE, frontend Specs и начало UX/reference HTML.
- Core DS 50.29.1 и assistant-ui 0.15.18 допускают React 19 (npm peerDependencies).

## Текущее состояние

- Созданы strict Vite приложение, DS corp theme, Query provider, маршруты /checks, /checks/:projectId и /checks/:projectId/chats/:threadId, 404.
- Docker nginx:8080 поддерживает SPA fallback и /healthz.
- Scope — статический smoke shell, без backend, fake persistence и chat runtime.

## Проверки

- npm metadata — passed (network escalation).
- lint, typecheck, build — passed locally.
- 5 Vitest smoke tests — passed locally with NODE_OPTIONS=--experimental-require-module (host Node 22.11).
- docker build -t counterparty-web:a2 . — passed.

## Решения и допущения

- Требуется Node >=22.13: DS hooks require(ESM uuid), ESLint visitor-keys engine. Docker node:22-alpine проверяет поддерживаемую среду.
- Core DS объявляет React 19 support, но неиспользуемые react-popper/react-virtual имеют старые peer ranges. npm ci проходит; эти компоненты требуют отдельной проверки при интеграции.
- Runtime spike остаётся F0-06; assistant-ui peer metadata проверены, пакет пока не установлен.

## Следующее действие

Завершить scripts в Docker build stage и проверить nginx deep link/healthz.
