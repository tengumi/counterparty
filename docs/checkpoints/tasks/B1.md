# B1 — UI API shell

- Status: discovery
- Branch: agent/b1-ui-api-shell
- Worktree: .worktrees/b1-ui-api-shell
- Owner scope: `services/ui_api/**`, `docs/checkpoints/tasks/B1.md`
- Depends on: Wave A baseline `7525c8b`, `packages/contracts` v0
- Last commit: none

## Готово

- Проверены границы UI Backend и требования к самостоятельному Python-сервису.

## Текущее состояние

- Реализация `services/ui_api` ещё не создана.

## Проверки

- `git status --short` — passed: worktree чистый.
- `uv run ruff check .` — not run: проект ещё не создан.
- `uv run ruff format --check .` — not run: проект ещё не создан.
- `uv run mypy` — not run: проект ещё не создан.
- `uv run pytest` — not run: проект ещё не создан.
- `uv build` — not run: проект ещё не создан.
- `docker build` и container health smoke — not run: проект ещё не создан.

## Решения и допущения

- F0 shell включает только typed health endpoint; бизнес-маршруты, DI/БД и repositories
  откладываются до продуктового среза.
- Локальная зависимость contracts подключается через uv workspace source без изменения
  shared-пакета.

## Следующее действие

Создать минимальный FastAPI composition root, health contract, тесты и packaging.
