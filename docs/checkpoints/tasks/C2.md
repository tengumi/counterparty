# C2 — storage import vertical (DB-01, DB-02, IMP-01)

- Status: discovery
- Branch: agent/c2-storage-import
- Worktree: .worktrees/c2-storage-import
- Owner scope: `migrations/**`, `packages/storage/**`, `scripts/import_reports/**`,
  `docs/checkpoints/tasks/C2.md`
- Depends on: A1 (contracts v0), A3 audit (checkpoint файл отсутствует, аудит
  выполнен заново read-only по mock JSON)
- Last commit: none

## Готово

- (пусто)

## Текущее состояние

- Discovery по `artifacts/contractors_audit.snapshot.json` выполнен.
- Extended JSON wrappers в файле только два: `$date` (4672) и `$numberLong` (67).
- `_id` — составной объект `{ogrn, date}`, а не ObjectId; `_id.date == report.reportDate`
  во всех 100 записях, `_id.ogrn == baseInfo.ogrn`; 100 уникальных ИНН/ОГРН.
- Контрольные значения Specs §10 подтверждены для ИНН `7449088645`:
  `finReports[0].common.year=2025`, `proceeds=74586000`, `capitals=-300000`,
  `bankroll=355000`.
- Все `$date` — локальная полночь: 4582 в `T21:00:00Z` (UTC+3) и 90 в `T20:00:00Z`
  (UTC+4). Календарную дату без угадывания таймзоны получить нельзя.

## План

1. DB-01: `packages/storage` metadata + naming convention, `migrations` как
   отдельный uv-проект, ревизия схем `reports`/`workspace`.
2. DB-02: ORM-модели и ревизия первой вертикали report-таблиц.
3. IMP-01: `scripts/import_reports` — Extended JSON decoder, source-state probe,
   schema fingerprint; тест на контрольных значениях.
4. Проверки: ruff/mypy/pytest в трёх проектах, upgrade/downgrade на временном
   PostgreSQL в Docker.

## Проверки

- не запускались

## Следующее действие

Создать metadata и модели в `packages/storage`.
