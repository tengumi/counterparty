# C3 — Domain: идентификаторы, значения и evidence ledger

- Status: discovery
- Branch: agent/c3-domain-evidence
- Worktree: .worktrees/c3-domain-evidence
- Owner scope: packages/domain/**, docs/checkpoints/tasks/C3.md
- Depends on: contract v0 в packages/contracts (read-only)
- Last commit: none

## Готово

- Прочитаны разделы data/evidence Specs 02 и 10 и реализованный contract v0.

## Текущее состояние

- packages/domain содержит только smoke-тест импорта.
- Решено опереться на contracts (Availability, EvidenceKind, EvidenceRef) вместо дублирования.

## План

1. Подключить counterparty-contracts как локальную зависимость domain.
2. `errors.py` + `facts.py`: FactSlot и явная семантика missing / present_empty / invalid / restricted vs zero.
3. `identifiers.py`: ИНН 10/12, ОГРН 13, ОГРНИП 15, КПП — формат и контрольные суммы.
4. `values.py`: Decimal/date/year helpers без float для денег.
5. `evidence.py`: EvidenceLedger, разрешимость refs (в т.ч. derived-цепочек), проверка grounding.

## Проверки

- `uv run pytest` — passed (baseline smoke).

## Решения и допущения

- domain не дублирует валидацию contracts, а надстраивается над ней.

## Следующее действие

Добавить зависимость на contracts и реализовать errors.py + facts.py.
