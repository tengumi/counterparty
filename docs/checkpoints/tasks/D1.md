# D1 — report and REST DTO (C-01, C-02)

- Status: discovery
- Branch: agent/d1-report-rest-contracts
- Worktree: .worktrees/d1-report-rest-contracts
- Owner scope: `packages/contracts/**`, `docs/checkpoints/tasks/D1.md`
- Depends on: A1 (contract v0), C2 (reports storage schema, source fixture values)
- Last commit: none

## Готово

- Прочитаны Specs 10 (§2–§5, §8, §12), 02 §3, 03, contract v0, `reports/models.py`,
  `counterparty_domain.FactSlot`, `import_reports.REPORT_SECTIONS`.

## Текущее состояние

- Реализации ещё нет.

## План

1. `values.py` + `facts.py`: DecimalString/Money/FiscalYear и `FactValue` с
   availability-инвариантами, согласованными с `FactSlot`.
2. `reports.py` (C-01): CompanyOverview, raw enum assessments, ReportSection с
   discriminated union записей, Comparison.
3. `projects.py` (C-02): Project/ProjectCompany/ThreadSummary/ProjectFact/
   UserDecision и REST request/response DTO §5.
4. Тесты на инварианты (missing≠zero≠empty≠invalid, unknown raw enum, Decimal,
   evidence-обязательность, fixture из Specs §10) и все проверки.

## Проверки

- не запускались

## Следующее действие

Реализовать шаг 1 (`values.py`, `facts.py`).
