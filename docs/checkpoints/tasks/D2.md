# D2 — WEB-02 (S1) и WEB-03 (каркас S2)

- Status: done
- Branch: agent/d2-web-screens
- Worktree: .worktrees/d2-web-screens
- Owner scope: apps/web/**, docs/checkpoints/tasks/D2.md
- Depends on: WEB-01 (app shell), C1 (демо-чат на транспорте агента)
- Last commit: 44ca67d

## Готово

- `src/mocks/{types,workspace}.ts` — единственный источник typed mock-данных проверок, компаний и чатов.
- S1: поле задачи (Enter/Shift+Enter, пустая отправка запрещена), примеры-подсказки, список проверок с пустым состоянием и поиском от 6 записей.
- S2: шапка (назад, название, состояние сохранения, переключатель чатов, «Материалы»), company context strip с «ещё N»/«Добавить»/«Сравнить» и статусом проекта, каркас панели материалов.
- Responsive: 1440 (панель 400 px рядом), 1024 (банковская навигация свёрнута), 390 (панель и список чатов полноэкранные).
- 27 Vitest-тестов, включая пустой список, длинные названия, переключение и создание чатов.

## Текущее состояние

- Работает: маршруты `/checks`, `/checks/:projectId`, `/checks/:projectId/chats/:threadId`; демо-чат C1 остался на `/checks/demo-project/chats/demo-thread`.
- Не работает намеренно: содержимое панели материалов (WEB-05/06), лента разговора и composer-состояния других чатов (WED-04), сохранение (WEB-08). Экран честно пишет, что сведения не загружены.

## Проверки

- `npm run lint` — passed
- `npm run typecheck` — passed
- `npm run test` — passed (27)
- `npm run build` — passed
- Ручной прогон в Chrome на 1440/1024/390 px — passed, ошибок в консоли нет.

## Решения и допущения

- «Прикрепить» в S1-02 не реализовано: без upload-адаптера кнопка была бы декоративной. Вложения — вместе с composer в WEB-04.
- S1-06 (переименование/удаление записи) не входит в WEB-02 и требует мутаций; отложено.
- Первая отправка не создаёт проверку: сервера нет. Черновик передаётся в S2 через router state и показывается как неотправленный.
- «Новый чат» создаёт чат только в состоянии клиента; после перезагрузки его нет, проект не меняется.
- Мок S1 сознательно содержит 3 проверки (как в baseline), поэтому поиск скрыт; порог 6 проверен тестом.
- Кнопка «Материалы» использует secondary + aria-expanded, а не брендовый красный (07 §12).

## Следующее действие

Главному агенту интегрировать 44ca67d. Запуск: `cd apps/web && npm ci && npm run dev`, открыть /checks. Далее WEB-04 (лента и composer) и WEB-05 (наполнение панели) поверх этой ветки.
