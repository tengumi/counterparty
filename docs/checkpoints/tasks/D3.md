# D3 — Domain: детерминированные расчёты по финансам и производствам

- Status: discovery
- Branch: agent/d3-domain-calculations
- Worktree: .worktrees/d3-domain-calculations
- Owner scope: packages/domain/**, docs/checkpoints/tasks/D3.md
- Depends on: C3 (facts/values/evidence), packages/contracts v0 (read-only)
- Last commit: none

## Готово

- (пока ничего)

## Текущее состояние

- Прочитаны C3-модули (`facts.py`, `values.py`, `evidence.py`), Specs 02 §3
  «Финансовые пути» + §8 и Specs 10 §2–3.

## Проверки

- `uv run ruff check .` — not run
- `uv run ruff format --check .` — not run
- `uv run mypy` — not run
- `uv run pytest` — not run

## Решения и допущения

- Строю поверх C3: `FactSlot`/`Availability`, `parse_decimal`/`sum_decimals`,
  `EvidenceLedger`. contracts не редактирую.

## План

1. `derivation.py` — привязка расчётного вывода к derived EvidenceRef.
2. `finance.py` — период, история периодов (год, не индекс), summary.
3. `proceedings.py` — сводка исполнительных производств.
4. `arbitration.py` — агрегаты без смешивания commonCount с детализацией.
5. Тесты на edge cases + все четыре проверки.

## Следующее действие

Реализовать `derivation.py` и арифметические помощники в `values.py`.
