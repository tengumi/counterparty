# E1 — workspace schema and async repositories (DB-03, DB-04)

- Status: discovery
- Branch: agent/e1-workspace-repositories
- Worktree: .worktrees/e1-workspace-repositories
- Owner scope: `migrations/**`, `packages/storage/**`, `docs/checkpoints/tasks/E1.md`
- Depends on: C2 (revisions 0001/0002, storage base), D1 (REST DTOs of projects)
- Last commit: none

## Готово

- Discovery: прочитаны Specs 02 §4/§6, Specs 03 (идемпотентность), C2/B2
  checkpoints, `reports/models.py`, `contracts/projects.py`.

## Текущее состояние

- Кода ещё нет.

## План

1. `packages/storage/workspace`: enums + модели (tenants, users, memberships,
   projects, threads, project_companies, idempotency_keys) поверх naming
   convention из `base.py`.
2. Ревизия `0003` — таблицы workspace; ревизия `0004` — роли и привилегии.
3. `packages/storage`: access scope, errors, async session factory,
   repositories и Unit of Work поверх reports + workspace.
4. Тесты: структурные (без БД) + интеграционные на реальном PostgreSQL,
   включая проверку падения запрещённой операции для каждой роли.
5. Прогон линтеров/типов/тестов и миграций на временном контейнере.

## Проверки

- не запускались

## Решения и допущения

- пока нет

## Следующее действие

Написать `packages/storage/src/counterparty_storage/workspace/models.py`.
