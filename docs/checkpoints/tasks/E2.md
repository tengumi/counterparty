# E2 — WEB-04 (разговор и composer) и WEB-05 (панель материалов)

- Status: implementation
- Branch: agent/e2-conversation-materials
- Worktree: .worktrees/e2-conversation-materials
- Owner scope: apps/web/**, docs/checkpoints/tasks/E2.md
- Depends on: D2 (S1/S2 каркас, mocks), C1 (транспорт агента)
- Last commit: none

## Готово

- (пока ничего)

## Текущее состояние

- Discovery завершён: прочитаны 06 §§2–8, 07 §§5–6, 11, 12, 16, 18 и структура
  разговора/панели в принятом HTML baseline.

## План

1. Типы разговора и материалов в `mocks/types.ts` + данные в `mocks/workspace.ts`.
2. WEB-04: блоки разговора (resume, реплика, notice, ход работы, ответ с
   основаниями, вывод, запрос подтверждения) и composer с состояниями;
   замена spike-composer в AgentChat без изменения транспорта.
3. WEB-05: навигация панели материалов (список групп → элемент → назад) и
   локальное сохранение drawer/черновика/прокрутки.
4. Тесты: пустой разговор, длинный ответ, terminal error, отмена во время run,
   восстановление черновика после перемонтирования.
5. lint/typecheck/test/build.

## Проверки

- `npm run lint` — not run
- `npm run typecheck` — not run
- `npm run test` — not run
- `npm run build` — not run

## Решения и допущения

- (заполняется по ходу)

## Следующее действие

Добавить типы разговора и материалов в `apps/web/src/mocks/types.ts`.
