# E3 — agent state, MCP contracts и demo auth (C-03, C-04, API-01)

- Status: discovery
- Branch: agent/e3-agent-mcp-contracts-api
- Worktree: .worktrees/e3-agent-mcp-contracts-api
- Owner scope: `packages/contracts/**`, `services/ui_api/**`,
  `docs/checkpoints/tasks/E3.md`
- Depends on: D1 (report/REST DTO), C1 (проекция состояния агента), B3 (MCP-шелл)
- Last commit: none

## Готово

- (пока ничего)

## Текущее состояние

- Discovery завершён: прочитаны Specs 03/04/05/10, `packages/contracts`,
  `services/agent/.../public_state.py`, `services/mcp/.../models.py`,
  `services/ui_api`.
- `packages/domain` не конвертирует `FactSlot` в `FactValue` и не импортирует
  `warnings` из contracts, поэтому типизация `warnings[]` в contracts не
  требует правок domain.
- Схемы `workspace` в `packages/storage` ещё нет: владение проектом в ui_api
  проверяется через порт (Protocol) с in-memory demo-реализацией.

## Проверки

- `uv run pytest` в `packages/contracts` — passed (100 tests, baseline)

## Решения и допущения

- План: (1) типизированный `warnings[]`, (2) C-03 `agent_state.py`,
  (3) C-04 `mcp.py`, (4) API-01 demo auth/session + ownership dependency.

## Следующее действие

Ввести `WarningCode`/`ContractWarning` и перевести `warnings[]` в contracts.
