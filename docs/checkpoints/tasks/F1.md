# F1 — идемпотентный импорт и slim compose (IMP-02, IMP-03, OPS-01 slim)

- Status: discovery
- Branch: agent/f1-import-and-compose
- Worktree: .worktrees/f1-import-and-compose
- Owner scope: `scripts/**`, корневой `compose.yaml`, `docs/checkpoints/tasks/F1.md`
- Depends on: C2 (revisions 0001/0002, `import_reports` decoder/fingerprint),
  E1 (роли, revisions 0003/0004), E3 (`ContractWarning`)
- Last commit: none

## Готово

- Пока ничего: прочитаны Specs §02, §10 (DTO/контрольные значения), C2, E1,
  модели `packages/storage/reports`, `roles.py` и существующий `import_reports`.

## Текущее состояние

- `scripts/import_reports` умеет только read-only inspect; записи в БД нет.
- Корневого `compose.yaml` нет.

## План

1. IMP-02: normalizer + writer + CLI-подкоманда `import`, unit-тесты без БД.
2. IMP-02: идемпотентность на `UNIQUE(sha256)` батча и `UNIQUE(company_id, hash)`
   снапшота, warnings через `ContractWarning` в `reports.import_warnings`.
3. IMP-03: прогон 100 снапшотов в живой PostgreSQL (`cp_import`) под ролью
   `counterparty_importer`, сверка контрольных значений C2/Specs §10.
4. OPS-01 slim: корневой `compose.yaml` (postgres + volume, migrate, importer).

## Проверки

- не запускались

## Решения и допущения

- Целевые числа источника: 100 записей, 100 уникальных ИНН, 2228 activity codes,
  194 finReports, 19 секций × 100 = 1900 строк `section_availability`.

## Следующее действие

Написать `normalize.py` и `warnings.py` в `scripts/import_reports`.
