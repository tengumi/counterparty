# F2 — project и company REST API (API-02, API-03)

- Status: discovery
- Branch: agent/f2-project-and-company-api
- Worktree: .worktrees/f2-project-and-company-api
- Owner scope: `services/ui_api/**`, `docs/checkpoints/tasks/F2.md`
- Depends on: E3 (demo auth, ownership dependency), E1 (workspace models,
  репозитории, UoW), D1 (REST DTO)
- Last commit: none

## Готово

- Ничего: идёт discovery.

## Текущее состояние

- Прочитаны Specs 03 и REST-раздел 10, checkpoints E1/E3, `packages/storage`
  (модели workspace, репозитории, UoW) и `packages/contracts/projects.py`.
- Тестовая база `cp_uiapi` создана в контейнере `cp-e1-pg`.

## План

1. Подключить `counterparty-storage` к `services/ui_api`: engine в lifespan,
   request-scoped UoW, `ProjectDirectory` поверх репозиториев.
2. API-02: `POST/GET /projects`, `GET/PATCH /projects/{id}` + первый thread;
   идемпотентность через `workspace.idempotency_keys`.
3. API-03: `GET /companies`, `POST /projects/{p}/companies`,
   `DELETE /projects/{p}/companies/{c}` с закреплением `report_id`.
4. Тесты на живом PostgreSQL + ruff/mypy/pytest + docker build.

## Проверки

- не запускались.

## Решения и допущения

- Пока нет.

## Следующее действие

Подключить `counterparty-storage` к `services/ui_api` и завести
request-scoped Unit of Work.
