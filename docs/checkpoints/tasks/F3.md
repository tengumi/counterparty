# F3 — WEB-06 (отчёт компании и детальное представление основания)

- Status: discovery
- Branch: agent/f3-web-company-report
- Worktree: .worktrees/f3-web-company-report
- Owner scope: apps/web/**, docs/checkpoints/tasks/F3.md
- Depends on: E2 (WEB-04/WEB-05: MaterialsPanel, materialsView, persisted, mocks)
- Last commit: none

## Готово

- Прочитаны 06 §4–§6, 07 §6 (P1-02/P1-03), §9 (границы данных), §10 (демо-история),
  чекпоинт E2 и текущий код панели.
- Из `artifacts/contractors_audit.snapshot.json` вынуты правдоподобные значения
  для ИНН 7449088645 (пример А по 07 §10), а также кандидаты на состояния:
  5029069967 (нет финансовых отчётов, riskLevel UNKNOWN) и 7728380537
  (`zskRiskLevel: YELLOW` — сырое значение без подтверждённого маппинга).

## Текущее состояние

- Панель умеет: список групп → элемент (компания/основание/документ/итог).
  Экран компании — заглушка, основание не ведёт к первоисточнику.

## Проверки

- не запускались (шаг discovery)

## Решения и допущения

- Продолжаю стек навигации WEB-05, вторую поверхность не строю: отчёт живёт в
  экране `company`, первоисточник — третий уровень того же стека.

## План

1. Типы отчёта (`ReportFact`, `FactState`, `ReportSection`, `CompanyReport`) и
   моки в `src/mocks/reports.ts`; основание выводится из факта, а не пишется
   отдельно.
2. Чистый модуль представления `companyReport.ts`: разрешение факта в основание,
   раскладка ЗСК и оценки банка.
3. `CompanyReport.tsx` внутри панели + расширенный экран основания.
4. Компонентные тесты: missing/zero/empty/unavailable, раскрытие значения в
   основание, сохранение raw ЗСК.
5. lint / typecheck / test / build.

## Следующее действие

Шаг 1: описать типы отчёта и `src/mocks/reports.ts`.
