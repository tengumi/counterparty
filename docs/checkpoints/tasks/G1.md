# G1 — overview and comparison

- Status: done
- Branch: agent/g1-overview-and-comparison
- Worktree: .worktrees/g1-overview-and-comparison
- Owner scope: packages/domain/**, services/ui_api/**, docs/checkpoints/tasks/G1.md
- Depends on: baseline 66b4de7; API-03, D-03, C-04
- Last commit: 48d59e9

## Готово

- Реализован D-04: стабильный порядок, year policies и честные per-row statuses без score/ranking.
- Добавлены `GET /api/v1/reports/{report_id}/overview` и `POST /api/v1/projects/{project_id}/comparisons` по pinned report IDs.
- Overview публикует identity/status/bank/ZSK, финансовые периоды, proceedings summary, completeness и стабильные report evidence ids.
- Comparison проверяет project membership, возвращает 2–20 строк в порядке запроса и сохраняет INVALID snapshot как unavailable row.

## Текущее состояние

- D-04/API-04/API-05 готовы; contracts/storage/Specs не менялись.

## Проверки

- `packages/domain: uv run ruff format --check . && uv run ruff check . && uv run mypy && uv run pytest` — passed: 110 tests.
- `services/ui_api: uv run ruff format --check . && uv run ruff check . && uv run mypy` — passed.
- `services/ui_api: COUNTERPARTY_TEST_DATABASE_URL=... uv run pytest` — passed: 44 tests на PostgreSQL, без skip.

## Решения и допущения

- Contracts/storage/Specs остаются read-only; недостающий примитив будет оформлен как точный gap.
- REST body использует существующий shared `CompareCompaniesInput`; отдельного REST request DTO в contracts нет, форма совпадает со Specs.
- Пока секция не имеет typed storage projection, comparison честно возвращает её availability; proceedings агрегируются из immutable raw JSON через domain rule.
- После merge domain требуется `uv sync --reinstall-package counterparty-domain` в venv UI API.

## Следующее действие

Интегрировать 71c5f3d, затем 48d59e9; повторить reinstall локального domain package и проверки UI API.
