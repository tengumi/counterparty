# G2 — live project/company REST slice

- Status: done
- Branch: agent/g2-web-live-api
- Worktree: .worktrees/g2-web-live-api
- Owner scope: apps/web/**, docs/checkpoints/tasks/G2.md
- Depends on: API-02/API-03, baseline 66b4de7
- Last commit: 6ba6fdb

## Готово

- Прочитаны guide и целевые разделы Specs 06/07/10.
- Добавлен typed REST client/config для project/company API и query cache boundary.
- S1 загружает и создаёт проверки; S2 загружает/переименовывает проект и изменяет состав компаний.
- Loading, retryable errors, unavailable и per-item add outcomes показаны явно.

## Текущее состояние

- Project/company composition приходит из REST; conversation, materials content, report overview and comparison remain typed mocks by scope.

## Проверки

- `npm run lint` — passed
- `npm run typecheck` — passed
- `npm test` — passed: 74 tests
- `npm run build` — passed

## Решения и допущения

- Overview, comparison, conversations and materials remain typed mocks; G2 only replaces project/company CRUD reads used by S1/S2.
- Browser screenshots are intentionally deferred to WEB-07.
- Live requests are same-origin by default; `VITE_UI_API_BASE_URL` overrides the base and local Vite proxies `/api/v1` to ui_api on port 8000.
- Existing assistant-ui transport test emits a nested `<p>` hydration warning; it predates G2 and tests still pass.

## Следующее действие

Интегрировать commits G2 после backend G1/G3 review; затем проверить live slice с действующей demo session.
