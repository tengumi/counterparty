# G3 — storage API и безопасный lifecycle ролей

- Status: done
- Branch: agent/g3-storage-and-role-debt
- Worktree: .worktrees/g3-storage-and-role-debt
- Owner scope: packages/storage/**, migrations/**, docs/checkpoints/tasks/G3.md
- Depends on: baseline 66b4de7; DB-03/DB-04/F1/F2
- Last commit: 8507fb3

## Готово

- Repository primitives реализованы и зафиксированы в `ef1475d`.
- Downgrade 0004 больше не удаляет cluster-wide роли; исправление и двухбазовый regression test зафиксированы в `8507fb3`.

## Текущее состояние

- Task scope полностью проверен; integration wiring в `services/ui_api` оставлен главному интегратору.

## Проверки

- `packages/storage: uv run ruff check .` — passed
- `packages/storage: uv run ruff format --check .` — passed
- `packages/storage: uv run mypy` — passed, strict
- `packages/storage: uv run pytest -q` — passed, 80 tests on PostgreSQL
- `migrations: uv run ruff check . && uv run ruff format --check . && uv run mypy` — passed
- migration tests на двух БД одного кластера — passed, 18 tests

## Решения и допущения

- Изменение shared-пакета потребует `uv sync --reinstall-package counterparty-storage` в потребляющих service venv после интеграции; чужие venv/lockfiles не меняются.
- `stale_after=None` не перехватывает ключ по возрасту; явный положительный threshold делает takeover атомарным.
- Alembic downgrade снимает grants только в своей БД; NOLOGIN-роли удаляются лишь отдельной cluster-decommission операцией.

## Следующее действие

Интегрировать `ef1475d`, затем `8507fb3`; переустановить storage в service venv и выполнить API wiring.
