# G4 — подключение storage primitives в UI API

- Status: verification
- Branch: agent/g4-api-storage-integration
- Worktree: .worktrees/g4-api-storage-integration
- Owner scope: services/ui_api/**, docs/checkpoints/tasks/G4.md
- Depends on: baseline 7ed360f / G3; G5 disposable PostgreSQL
- Last commit: 9e4c382 (discovery); implementation commit follows

## Готово

- GET /projects использует owner/title-filtered list_recent, query доступен в REST.
- Reads использует batch membership и CompanyReadRepository.search, дубли SQL убраны.
- Release вызывает repository после rollback; completed key сохраняется.
- Добавлены HTTP regressions фильтра/пагинации, same-tenant owner, latest/pinned identity, missing report, stale in-flight и before/after commit failure.

## Текущее состояние

- Реализация и static checks готовы; G5 собирает PostgreSQL для отдельной БД g4_api.
- Shared contracts/domain/storage переустановлены в своём venv. Python 3.13 игнорировал editable .pth из-за macOS UF_HIDDEN; локальный chflags nohidden исправил import.

## Проверки

- `git status --short` — clean, нужная task branch.
- `ui_api: uv run --no-sync ruff check .` — passed.
- `ui_api: uv run --no-sync ruff format --check .` — passed, 25 files.
- `ui_api: uv run --no-sync mypy` — passed, 25 files.
- `ui_api: uv run --no-sync pytest -q` — 17 passed, 33 skipped (DB URL pending).
- `git diff --check` — passed; изменён только ownership.

## Решения и допущения

- `stale_after=None`: возраст запроса не доказывает остановку первого worker; без fencing/reconciliation takeover может создать два проекта. Старый in-flight остаётся 409 retryable. Crash recovery вне G4.
- Threads count SQL остаётся в read adapter: готового batch primitive нет.

## Следующее действие

Получить COUNTERPARTY_TEST_DATABASE_URL от G5 и выполнить полный ui_api pytest; исправить failures перед handoff.
