# G4 — подключение storage primitives в UI API

- Status: implementation
- Branch: agent/g4-api-storage-integration
- Worktree: .worktrees/g4-api-storage-integration
- Owner scope: services/ui_api/**, docs/checkpoints/tasks/G4.md
- Depends on: baseline 7ed360f / G3; G5 disposable PostgreSQL
- Last commit: none

## Готово

- Прочитаны UI Backend, REST §5 и G3 checkpoint; найдены дубли SQL в reads/idempotency.

## Текущее состояние

- План: подключить project list/query, batch membership и company search; заменить release; проверить HTTP regression и полный ui_api набор.
- Первый следующий шаг: переустановить локальные shared packages и заменить дубли готовыми repositories.

## Проверки

- `git status --short` — clean, нужная task branch.
- API checks — not run: implementation pending; G5 готовит отдельную test DB.

## Решения и допущения

- `stale_after=None`: возраст запроса не доказывает остановку первого worker; без fencing/reconciliation takeover может создать два проекта. Старый in-flight остаётся 409 retryable. Crash recovery вне G4.
- Threads count SQL остаётся в read adapter: готового batch primitive нет.

## Следующее действие

Подключить primitives и добавить HTTP проверки фильтра/пагинации, владельца, старого in-flight и безопасного release.
