# G5 — независимая проверка данных и storage

- Status: discovery
- Branch: agent/g5-data-verification
- Worktree: .worktrees/g5-data-verification
- Owner scope: docs/checkpoints/tasks/G5.md
- Depends on: baseline 7ed360f (G1/G3), затем G4
- Last commit: none

## Готово

- Прочитаны acceptance и checkpoints G1/G3; проверяемый worktree чистый.
- Storage/API fixtures пересоздают schemas; migration regression требует две БД одного кластера. Общая/demo БД использоваться не будет.

## Текущее состояние

- Локальных PostgreSQL binaries и Python 3.13 в проверенных `.tools`, Homebrew и `/tmp` нет; доступны uv, compiler и make.
- План: (1) подготовить disposable PostgreSQL и выделить G4 отдельную БД; (2) переустановить shared packages; (3) выполнить пять локальных quality suites, включая все PostgreSQL tests и двухбазовый regression; (4) независимо проверить G4 по follow-up.

## Проверки

- `git status --short` — passed, чисто.
- ruff / mypy / pytest — not run: готовятся prerequisites.

## Решения и допущения

- Runtime prerequisites живут только в ignored `.tools` или `/tmp`; исходники проекта read-only.
- Для storage, importer, migration pair и G4 будут отдельные disposable БД.

## Следующее действие

Подготовить PostgreSQL 17 и Python 3.13, затем передать G4 адрес выделенной БД.
