# G5 — независимая проверка данных и storage

- Status: verification
- Branch: agent/g5-data-verification
- Worktree: .worktrees/g5-data-verification
- Owner scope: docs/checkpoints/tasks/G5.md
- Depends on: baseline 7ed360f (G1/G3), затем G4
- Last commit: 8342954

## Готово

- Прочитаны acceptance и checkpoints G1/G3; проверяемый worktree чистый.
- Storage/API fixtures пересоздают schemas; migration regression требует две БД одного кластера. Общая/demo БД использоваться не будет.
- PostgreSQL 17.6 собран из официального release source в `/private/tmp/g5-postgres`, cluster `/private/tmp/g5-pgdata`, localhost:55435. G4 получил отдельную `g4_api`.
- Python 3.13.15: `/private/tmp/g5-python/cpython-3.13.15-macos-aarch64-none/bin/python3.13`; venv созданы через `uv sync --frozen --python <path>` с переустановкой local shared packages.

## Текущее состояние

- contracts/domain/migrations проверены; importer static checks прошли. Storage найден locale-dependent failure: `--locale=C` не приводит кириллицу к lowercase. Создана отдельная `g5_storage_utf8` с `TEMPLATE template0 LOCALE 'en_US.UTF-8'` для корректного повтора.
- Следом: importer под ограниченной ролью, отдельная demo DB `g5_demo_utf8` для G6, независимый review G4.

## Проверки

- `git status --short` — passed, чисто.
- Во всех пяти проектах `UV_CACHE_DIR=/private/tmp/g5-uv-cache uv run --frozen ruff check .`, `ruff format --check .`, `mypy` — passed.
- contracts `uv run --frozen pytest -q` — passed, 135.
- domain `uv run --frozen pytest -q` — passed, 110.
- migrations `COUNTERPARTY_TEST_DATABASE_URL=postgresql+psycopg://g5_admin@127.0.0.1:55435/g5_migrations COUNTERPARTY_TEST_SECOND_DATABASE_URL=postgresql+psycopg://g5_admin@127.0.0.1:55435/g5_migrations_second uv run --frozen pytest -q` — passed, 18, без skip.
- storage против `g5_storage` (`LC_CTYPE=C`) — 79 passed, 1 failed (`test_company_search_uses_latest_local_profile_and_stable_cursor`, кириллический ILIKE); повтор с UTF-8 locale предстоит.
- Sandbox первоначально блокировал download, PostgreSQL shared memory и localhost TCP; штатная escalation разблокировала окружение. Эти connection errors не считаются проверкой кода.

## Решения и допущения

- Runtime prerequisites живут только в ignored `.tools` или `/tmp`; исходники проекта read-only.
- Для storage, importer, migration pair и G4 будут отдельные disposable БД.
- Для Cyrillic case-insensitive search нужна Unicode-aware LC_CTYPE; `SELECT lower('АЛЬФА'), 'АЛЬФА' ILIKE '%альфа%'` в `g5_storage_utf8` вернул `альфа, true`.
- Docker отсутствует; Dockerfiles не менялись и Docker build не запускался.

## Следующее действие

Повторить storage tests на `g5_storage_utf8`, затем мигрировать importer/demo и выполнить импорт под ограниченной ролью.
