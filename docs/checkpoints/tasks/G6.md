# G6 — проверка web и подготовка WEB-07

- Status: done — pass with limitations (browser/Docker gate не проверен)
- Branch: agent/g6-web-verification
- Worktree: .worktrees/g6-web-verification
- Owner scope: docs/checkpoints/tasks/G6.md, README.md; apps/web read-only
- Depends on: baseline 7ed360f, G2; G4 0bb8aaa merged (1964105); G5 demo DB
- Last commit: 4a32d0e (pre-smoke checkpoint); результат — финальный commit G6

## Готово

- Четыре web checks прошли; package.json/package-lock.json и runtime-код не изменены.
- README отражает реальный срез и содержит запуск slim Compose → login с ролью
  `counterparty_ui_api` → UI API → Vite → browser demo session.
- REST config/proxy/auth сверены с кодом: same-origin `/api/v1`, API порт 8000,
  `credentials: include`, `UI_API_SESSION_COOKIE_SECURE=false` для local HTTP.

## Текущее состояние

- Live: список/создание/чтение/rename проектов; add/remove компаний с context_version.
- Typed mocks: chats/conversation/report/evidence/materials/summary. Новый чат
  создаётся локально; сравнение пока только открывает материалы, таблицы нет.
- Реальные UUID не совпадают с typed mock IDs: REST-проект показывает пустой
  разговор/недоступного помощника и не подключённые детали отчёта.
- `VITE_UI_API_BASE_URL` для локального запуска не нужен. Отдельный cross-origin
  URL не проверен; CORS middleware в API нет. Nginx образа web не проксирует API.
- `/rpc/agent` в Vite всё ещё указывает на порт 8000; UI API не обслуживает RPC.
  Это остаётся стыком WEB-09/OPS-01 и не требуется для live CRUD.

## Проверки

- Toolchain: локальный Node 24.19.0; npm 10.9.2 скачан в ignored `.tools/npm`.
- `npm ci --no-audit --no-fund` — passed: 707 packages, lockfile неизменён.
- `npm run lint` / `npm run typecheck` — passed.
- `npm test` — passed: 74 tests, 10 files; fetch в unit tests подменён.
- `npm run build` — passed: Vite 6.4.3.
- `git diff --check` — passed.
- `uv sync --frozen` + import UI API — passed, отдельный G6 venv.
- Exact API source: G4 `0bb8aaa` (implementation `37e1b25`), merged в G6
  как `1964105`; последующие G6 commits меняют только документацию.
- Docker/browser — not run: Docker отсутствует, браузер отложен по scope.
- Native PostgreSQL 17.6, `g5_demo_utf8` (en_US.UTF-8, 100 snapshots),
  login `g5_web_login` с membership `counterparty_ui_api`, без superuser.
- `services/ui_api/.venv/bin/python .tools/http_smoke.py http://127.0.0.1:8000`
  — passed: 17 HTTP checks (health, unauth 401, session, create/replay, list,
  rename, add 2 + not_found, reload, pinned overview с 20 refs, comparison
  2 rows в запрошенном порядке, remove/reload, чужой tenant 404).
- Vite HTTP proxy — passed: `/checks` 200; POST `/api/v1/auth/session` 201
  с HttpOnly cookie; GET `/api/v1/projects` 200 через `localhost:5173`.
  Browser/DOM не использовался; клиент smoke в ignored `.tools/http_smoke.py`.

## Решения и допущения

- Сохранились nested `<p>` warning в transport test, warning о JS chunk 726 KB
  и transitive peer warnings react-popper/react-virtual для React 19. Обновлений
  dependencies ради подавления warnings нет.
- Compose выдаёт LOGIN только importer; runbook создаёт отдельный demo login
  `counterparty_web_dev` с GRANT `counterparty_ui_api`, без superuser для API.

## WEB-07 handoff

- Результат: сравнить S1/S2 с неизменяемым HTML reference на 390/1024/1440 px,
  сохранить screenshots и компактный список расхождений/исправлений.
- Входы: Specs 06 §7 и 07; принятый HTML; существующие typed fixtures в
  `apps/web/src/mocks/` и REST fixtures из `apps/web/src/test/setup.ts`.
- Для полного визуального S2 нужен browser-test harness/intercept REST с этими
  fixtures: `/checks/demo-project/chats/demo-thread` не является live API route.
  Не добавлять production fallback и искусственные report IDs в БД.
- Покрыть S1 empty/list/loading/error; S2 conversation/composer, chat switcher,
  открытые materials/report/evidence; длинные названия, focus/keyboard,
  сохранение draft/scroll после открытия и закрытия panel.
- Отдельно live CRUD smoke через demo session: создание/rename, add/remove,
  частичный not_found, перезагрузка проекта; ошибки не превращаются в empty.
- Не засчитывать mock screenshots как WEB-08/09. WEB-08 заменяет остаточные
  report/materials/comparison mocks; агентный reconnect/cancel относится к WEB-09.

## Native запуск из dev

- Временные API PID 20853 и Vite PID 21046 остановлены после smoke; порты
  8000/5173 освобождены. DB G5, task branch/worktree сохраняются.
- PostgreSQL: `127.0.0.1:55435/g5_demo_utf8`, localhost trust, demo login
  `g5_web_login`; подготовка и lifecycle — G5. Это временное окружение.
- API source для повторного запуска — основной worktree `dev` с G4. Его
  `services/ui_api/.venv` уже подготовлен главным агентом. Используемый Python:
  `/private/tmp/g5-python/cpython-3.13.15-macos-aarch64-none/bin/python3.13`.

```sh
cd /Users/razi/Documents/ChatGPT/hack/services/ui_api
UI_API_DATABASE_URL='postgresql+psycopg://g5_web_login@127.0.0.1:55435/g5_demo_utf8' \
UI_API_SESSION_COOKIE_SECURE=false \
.venv/bin/uvicorn counterparty_ui_api.app:app --host 127.0.0.1 --port 8000 --no-access-log
```

Web из основного worktree, используя найденный Node и bootstrap npm G6:

```sh
cd /Users/razi/Documents/ChatGPT/hack/apps/web
export PATH=/Users/razi/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin:$PATH
node ../../.worktrees/g6-web-verification/.tools/npm/package/bin/npm-cli.js ci
node ../../.worktrees/g6-web-verification/.tools/npm/package/bin/npm-cli.js run dev -- --port 5173
```

Команды smoke выполнены из task worktree на том же коде web/G4; приведённые
выше пути адаптированы для повторного запуска из dev. Вход `demo-analyst` —
через browser fetch из README; сессии после остановки API сброшены. Сохранён
проект `06d4671a-50dc-4503-a156-3a90c9ebd294` («G6 HTTP smoke») с одной компанией.

## Следующее действие

Интегрировать документационные commits G6 после G4; следующий срез — WEB-07
по handoff выше. HTTP smoke подтверждает запуск, но не закрывает browser gate.
