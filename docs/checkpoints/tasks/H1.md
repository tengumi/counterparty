# H1 — WEB-07 visual alignment

- Status: verification
- Branch: agent/h1-web-visual-alignment
- Worktree: .worktrees/h1-web-visual-alignment
- Owner scope: apps/web/src runtime views/CSS and colocated tests; no API/mocks/transport/config
- Depends on: baseline 9e6a66a; H2 final browser QA; H3 independent review
- Last commit: a476590; tablet overlay-grid correction follows

## Готово

- Bounded viewport and live assistant-ui flex root keep composer visible; docked 400px panel spans S2, overlays tablet/working areas below 1000px, fills mobile.
- Compact materials/report rows, collapsible sections, readable evidence details, rounded prompt buttons, glyph send control and explicit assistant help link follow accepted HTML.
- Escape, overlay keyboard containment, focus return, preserved selected material/report sections/panel scroll and chat draft; «Обсудить» returns to composer.
- Mobile Enter creates a newline; send button is explicit. Unavailable agent preserves editable drafts.
- Restored conversation scroll survives first ResizeObserver notification. Invalid nested paragraphs from assistant-ui parts removed without transport changes.

## Текущее состояние

- H2 browser QA found tablet overlay width collapsing to 1px: absolute panel inherited desktop grid-column 2 after screen switched to one column. Both overlay breakpoints now reset grid-area to auto; desktop docked placement and mobile fixed bounds stay unchanged. No browser or screenshot capture was run in H1.
- Existing data/attachment/decision/comparison behavior retained; contextual discussion still inserts editable text, no new attachment API.

## Проверки

- `npm run lint` — passed.
- `npm run typecheck` — passed.
- `npm run test` — passed, 77 tests / 11 files; includes mobile Enter, panel Escape/reopen/focus, expanded report return and restored-scroll regression.
- `npm run build` — passed; existing >500kB bundle advisory remains.
- `git diff --check` — passed.
- Accessible-label follow-up: lint/typecheck and 17 focused S2/materials tests passed.

## Решения и допущения

- Checks used approved Node runtime and existing g6 dependencies via temporary local symlink; no package/lockfile changes.
- H3 static review accepted responsive/focus/scroll behavior; key title/company/switcher/panel/conversation controls now have 44px targets at every viewport.
- Panel close accessible name includes both visible labels: «Назад к разговору — закрыть материалы».
- On mobile, main disables query containment so fixed full-screen panels anchor to the viewport.
- Browser/rendered layout verdict belongs to H2; runtime API/mock/transport boundaries unchanged.

## Следующее действие

Root integrates overlay-grid correction; H2 reruns targeted tablet materials/report/source checks and confirms desktop/mobile bounds.
