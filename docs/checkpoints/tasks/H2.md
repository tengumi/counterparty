# H2 — воспроизводимый WEB-07 browser harness

- Status: implementation
- Branch: agent/h2-web-browser-harness
- Worktree: .worktrees/h2-web-browser-harness
- Owner scope: apps/web/qa/**, web QA package/config, src/test fixture extraction, artifacts/qa/WEB-07/**, этот checkpoint
- Depends on: baseline 9e6a66a; H1 integration signal; H3 acceptance matrix
- Last commit: none

## Готово

- Прочитаны G6 и Specs 07 §3–5; найден установленный Google Chrome.
- Browser navigation/screenshots до сигнала root после H1 не выполняются.

## Текущее состояние

- План: вынести shared REST fixtures из test setup; установить pinned Playwright; написать QA runner и manifest; static checks; после H1 — CDP/browser capture и live CRUD.
- Размеры: 390×844, 1024×768, 1440×900. Typed-fixture S1/S2/materials/report/evidence отделяются от live CRUD.

## Проверки

- `git status --short` — clean на старте; branch соответствует task.
- Browser — not run: ожидается H1 и сигнал root.

## Решения и допущения

- Только browser-test REST interception существующих fixtures. Production fallback и искусственные IDs в БД запрещены.
- PNG и manifest указывают источник данных и source SHA; mocked S2 не доказывает WEB-08/09.
- Chrome через стандартный Playwright/CDP, отдельный ephemeral profile.

## Следующее действие

Подготовить fixture extraction, pinned Playwright harness и выполнить lint/typecheck/test/build без browser navigation.
