# H2 — воспроизводимый WEB-07 browser harness

- Status: verification
- Branch: agent/h2-web-browser-harness
- Worktree: .worktrees/h2-web-browser-harness
- Owner scope: apps/web/qa/**, web QA package/config, src/test fixture extraction, artifacts/qa/WEB-07/**, этот checkpoint
- Depends on: baseline 9e6a66a; H1 integration signal; H3 acceptance matrix
- Last commit: 77b828f (discovery); harness implementation follows

## Готово

- Chrome 150.0.7871.115, Playwright Core 1.63.0; отдельный временный profile и стандартный connectOverCDP.
- REST fixtures вынесены из test setup, QA interception не меняет приложение/БД.
- Harness: `npm run qa:browser` — безопасный dry run; `--capture --mode=all` — reference + typed fixtures + live CRUD.
- Reference HTML/support.js не меняются; отдельные PNG S1/S2 на 3 viewport и materials desktop/mobile, source hashes в manifest.
- Browser navigation/screenshots до сигнала root после H1 не выполняются.

## Текущее состояние

- Harness прошёл static validation; ожидает merge H1 с последней mobile close label и final signal root.
- Согласованы H3 matrix, evidence center clicks/44px overlap, source state distinctions, restored scroll, focus return, 200% text-size emulation.
- Generated artifacts исключены из clean-source guard; последовательный live capture не блокируется PNG.
- Размеры: 390×844, 1024×768, 1440×900. Typed-fixture S1/S2/materials/report/evidence отделяются от live CRUD.

## Проверки

- `git status --short` — clean на старте; branch соответствует task.
- `npm run lint` / `npm run typecheck` / `npm run qa:check` — passed.
- `npm test` — 74 passed (pre-H1); `npm run build` — passed, известный chunk-size warning.
- `npm run qa:browser` — passed: matrix printed, browserStarted=false.
- Browser — not run: ожидается H1 и сигнал root.
- Package/lock diff: только playwright-core1.63.0, @types/node24.13.3 и его undici-types; существующие versions не менялись.

## Решения и допущения

- Только browser-test REST interception существующих fixtures. Production fallback и искусственные IDs в БД запрещены.
- PNG и manifest указывают источник данных и source SHA; mocked S2 не доказывает WEB-08/09.
- Chrome через стандартный Playwright/CDP, отдельный ephemeral profile.

## Следующее действие

Root интегрирует harness + H1, затем выдаёт signal; выполнить final `npm run qa:browser -- --capture --mode=all`, передать PNG/manifest H3 и исправить выявленные defects в своей области.
