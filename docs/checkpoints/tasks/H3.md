# H3 — независимая WEB-07 приёмка

- Status: done
- Verdict: pass with limitations — открытых WEB-07 blockers нет; ограничения покрытия ниже.
- Branch: agent/h3-web-acceptance-review
- Worktree: .worktrees/h3-web-acceptance-review
- Owner scope: docs/checkpoints/tasks/H3.md
- Depends on: baseline 9e6a66a; first capture b13cc17; final fixes/capture 6942d5a
- Last commit: e186e3a; финальный review — в commit этого checkpoint.

## Готово

- Прочитаны Specs 07 §3–6/12, 08 gates, принятый HTML и сопоставимые CSS/views.
- H1 получил приоритеты layout/viewport/panels/touch; H2 — покрытие ниже.
- H3 выполнил source/reference review и независимый просмотр сохранённых PNG/manifest H2; второй browser sweep не запускался.
- Проверены S1/S2/P1 на 390×844/1024×768/1440×900, report/evidence, mobile long-name/switcher, error и отдельный live CRUD screenshot.

## Acceptance matrix

| Gate | Evidence для pass | Источник |
|---|---|---|
| S1 | Заголовок→composer→history; пустая отправка disabled; два равновесных примера только вставляют текст; 0/6+ history и ошибка с retry отличимы | 07 §4; HTML S1 |
| S2 | Компактные header/company strip; title не вытесняет controls; feed scroll отдельно от закреплённого composer; один CTA текущего действия | 07 §3/5/12; HTML S2 |
| Desktop 1440×900 | Sidebar 224, chat text ≤760, обычная P1 400; одновременного подробного материала только один | 07 §3/6 |
| Tablet 1024×768 | Навигация свёрнута, P1 перекрывает чат; composer/controls не обрезаны | 07 §3 |
| Mobile 390×844 | P1 fullscreen с понятным назад/закрыть, chat switcher fullscreen; нет горизонтального overflow и трёх колонок | 07 §3/§18 |
| P1 переходы | List→company→evidence→source→back/close; company/period/value/source/date читаемы; возврат не теряет draft/scroll, focus возвращается инициатору | 07 §5–6/12 |
| Accessibility/errors | Клавиатурный путь, видимый focus, touch ≥44×44; 200% без обрезки кнопок; ошибки/unknown/empty различаются, draft сохраняется | 07 §11–12; 08 §3/V06 |

## Результат review

- Сохранены композиция принятого HTML, два маршрута, нейтральный shell, колонка текста ≤760, панель400 и один основной CTA текущего действия. Более высокие controls обоснованы touch44; responsive следует Specs, а не обрезанному mobile shell старого HTML.
- Bounded viewport/feed/dock, mobile title/fullscreen, panel scroll/expanded sections, initial auto-scroll restore, keyboard focus-return и raw source values проверены по source и browser evidence.
- Закрыты H3/root findings: минимальные touch targets, mobile close accessible name, tablet P1 сжатая до1px (grid-area reset), противоречивый mobile Enter hint. Два availability failures были harness selector на hidden duplicate; scoped assertions проверили реальные visible состояния.
- Отсутствие overflow, сохранение draft/scroll при panel/chat/reload, точные centers evidence1–4 без overlap подтверждены на трёх viewport; missing/empty/unavailable/zero визуально различаются.

## Проверки

- [Первый manifest](../../../artifacts/qa/WEB-07/manifest-all.json), SHA `b13cc17`: 27 PNG, 94 assertions (90 pass / 4 fail), consoleErrors=[]; исходные failures сохранены честно.
- [Tablet follow-up](../../../artifacts/qa/WEB-07/follow-up/manifest-tablet-s2.json), SHA `6942d5a`: 26/26 pass, 4 PNG, consoleErrors=[]; P1 фактически400px overlay, весь ранее блокированный navigation/draft/scroll/evidence path прошёл.
- [Availability/mobile hint follow-up](../../../artifacts/qa/WEB-07/follow-up/manifest-availability.json), SHA `6942d5a`: 13/13 pass, 3 PNG, consoleErrors=[]; H3 просмотрел изменённые поверхности. Все четыре первоначальных failed outcomes закрыты.
- H1/H2 integrated checks: 77 unit tests, lint/typecheck/qa:check/build passed (evidence исполнителей/root); H3 повторно не запускал неизменённые suites. `git diff --check` — passed.
- Browser: Chrome150.0.7871.115 / Playwright connectOverCDP, sourceDirty=false в manifests. Artifacts — 34 PNG с исходными SHA, первая и повторная серии не смешаны.

## Решения и ограничения

- Scope — WEB-07 visual/adaptive/current UI behavior. Не требуются WEB-08/09, live reports/agent, DOC, запись решения или новая функциональная волна.
- Mock PNG не доказывают backend/agent readiness. Specs важнее расхождений старого HTML (например touch44/adaptive).
- 200% проверка — эмуляция text-size на S1; native Chrome zoom, OS mobile keyboard и полный WCAG audit не выполнялись. Это ограничение coverage, не подтверждённый дефект текущего UI.
- Live CRUD проверял реальные REST и server UUID отдельно; сведения/материалы/разговор учебного примера остаются typed mocks. Документный preview честно сообщает недоступность чтения.

## Следующее действие

Главному агенту интегрировать H3 checkpoint и H2 artifacts/index, затем показать пользовательский WEB-07 срез и явно указать ограничения покрытия.
