# I1 — Report sections and project evidence

- Status: done
- Branch: agent/i1-report-read-api
- Worktree: .worktrees/i1-report-read-api
- Owner scope: services/ui_api/**; packages/domain/**; packages/contracts/** (ReportEvidence only)
- Depends on: baseline 1173476; shared projection consumed by I2
- Last commit: see branch HEAD (cursor parity follow-up after 57415fe)

## Готово

- Live GET reports/{r}/sections/{section}: all 17 section names, typed records/facts,
  contract filters, source-bound cursor, default20/max100, availability and warnings.
- Live GET projects/{p}/evidence/{ref:path}: project/tenant/user ownership,
  historical pinned report membership, issued-reference allowlist, 64KiB fragment cap.
- Overview moved to the same pure ReportReadData projection used by MCP; normalized
  columns remain authoritative. No UI API import from MCP, storage/migration changes.
- ReportEvidence DTO exported and directly tested. Original source fragment and exact
  report identity; raw zero, null, empty and missing remain distinguishable.
- Actual arbitration pf/df keys and source typo handled; status/year aggregates never added.

## Проверки

- contracts: uv run ruff check .; uv run ruff format --check .; uv run mypy; uv run pytest — passed,140 tests.
- domain: same four commands — passed,119 tests, including offset instant and REST/MCP cursor equivalence.
- ui_api: same four commands — passed,60 tests with the two URLs below.
- COUNTERPARTY_TEST_DATABASE_URL=postgresql+psycopg://g5_admin@127.0.0.1:55435/i1_api
- COUNTERPARTY_CORPUS_DATABASE_URL=postgresql+psycopg://g5_web_login@127.0.0.1:55435/g5_demo_utf8
- Optional test_report_corpus runs SET TRANSACTION READ ONLY. Approved100 reports /
  1700 sections:1028 available,454 missing,218 present_empty,0 invalid;14035 section refs exist.
- No Dockerfile change; Docker unavailable. Existing Starlette/httpx deprecations only.

## Решения и допущения

- Report corpus is shared and authenticated; project scope is enforced on evidence.
- Documents/artifact/user-message evidence not yet implemented; unsupported refs return404.
- Source calendar-only inspection dates cannot fill UTC-instant DTO fields: null with
  precision warning, original calendar values remain in evidence. No invented timezone.
- Oversized issued fragments return existing limit_exceeded409; arbitrary raw pointers404.
- Related company full-report availability remains null until a deliberate lookup is added.
- REST query names: repeated years, active, role, status_raw, cursor, limit. Unknown/unsupported keys422.

## Следующее действие

Root merge I1 branch after shared a05e0f9, reinstall local domain/contracts dependencies,
then I2 targeted parity verification and I4 frontend REST binding.
