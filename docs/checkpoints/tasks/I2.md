# I2 — MCP PostgreSQL report tools

- Status: implementation
- Branch: agent/i2-mcp-report-tools
- Worktree: .worktrees/i2-mcp-report-tools
- Owner scope: services/mcp/**, docs/checkpoints/tasks/I2.md
- Depends on: baseline 1173476; I1 shared domain read projection
- Last commit: f982bcd (discovery); I1 a05e0f9 merged

## Готово

- Прочитаны guide, Specs 05 и contracts 10 §3/8, B3, existing shared MCP DTO.
- Согласуется domain projection с I1; service-owned output stubs заменяются shared envelope.

## Текущее состояние

- Реализованы FastMCP TokenVerifier, shared tools/envelopes, read-only PostgreSQL adapter,
  lifecycle, semaphore/timeout/byte budgets и безопасная телеметрия.
- План: (1) config/auth/resource lifecycle и DB adapter; (2) typed overview/section tools;
  (3) стандартный HTTP MCP client smoke + negative/DB guards; (4) проверки и handoff.

## Проверки

- `uv run ruff check .`, `uv run ruff format .`, `uv run mypy` — passed.
- `uv run pytest tests/test_app.py tests/test_runtime.py tests/test_import.py` — 21 passed.
- Native DB i2_mcp создана; интеграционные проверки ещё не выполнялись.

## Решения и допущения

- MCP только reports; credentials server-side; отсутствующий credential закрывает доступ.
- Отдельная integration DB i2_mcp на native PostgreSQL :55435; demo DB не используется.
- Docker отсутствует, image rebuild будет отмечен непроверенным.

## Следующее действие

Добавить PostgreSQL fixtures/role guards и выполнить реальные snapshot/section checks.

- Auth API evidence: https://gofastmcp.com/servers/auth/token-verification (FastMCP3.4.7, 2026-09-05); standard TokenVerifier extension, no custom transport.
