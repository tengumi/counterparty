# I3 — PostgreSQL checkpoint and restart spike

- Status: discovery
- Branch: agent/i3-agent-checkpoint-restart
- Worktree: .worktrees/i3-agent-checkpoint-restart
- Owner scope: services/agent/**, migrations/**, this checkpoint; contracts/storage read-only
- Depends on: baseline 1173476; native PostgreSQL 127.0.0.1:55435 (i3_agent/i3_migrations); root decision on missing durable run repository
- Last commit: none

## Готово

- Read required Specs04 §4/7, Specs10 §6/9 and V04 acceptance; inspected existing saver lifecycle and in-memory V01 registry.
- Confirmed historical agent_state recommendation is superseded: framework checkpoint tables must reside in workspace.

## Текущее состояние

- Saver factory opens PostgreSQL but has no explicit schema selection or real persistence test.
- Durable run model/repository is absent. Proposed minimal scoped run persistence to root before editing shared ownership.

## Проверки

- Not run yet; baseline discovery.

## Решения и допущения

- Plan: official saver deployment/setup + safe workspace placement; trusted scoped mapping; minimal approved lifecycle recovery boundary; real PostgreSQL restart/isolation/no-DDL proof; local service/migration quality checks.
- No AG-01/04 expansion, custom checkpoint format/engine, or agent loop. Docker unavailable.

## Следующее действие

Install service environment and inspect pinned AsyncPostgresSaver APIs while root resolves durable-run ownership.
