# I3 — PostgreSQL checkpoint and restart spike

- Status: verification
- Branch: agent/i3-agent-checkpoint-restart
- Worktree: .worktrees/i3-agent-checkpoint-restart
- Owner scope: services/agent/**, migrations/**; root additionally authorized minimal storage run models/enums/repositories/roles/exports/tests
- Depends on: baseline 1173476; native PostgreSQL 127.0.0.1:55435
- Last commit: 12935d8 (historical report scope helper); persistence implementation follows

## Готово

- Official AsyncPostgresSaver (locked 3.1.2) uses workspace-only search_path; explicit deploy command owns setup/grants, worker startup performs no DDL.
- Alembic excludes exact library-owned table names in workspace; 0005 owns only durable agent_runs and scoped composite FKs/one-active-run constraint.
- 0004 initial agent grants frozen so fresh upgrade does not reference future tables; final privileges match shared matrix.
- AgentRunOwner holds one PG advisory lock and serializes recovery/writes on that same dedicated connection. Second worker fails before recovery; killed owner cannot reconnect and write.
- Scoped run repository verifies tenant/project/thread and active, non-deleted project mapping before deriving a stable saver key. Recovery marks active abandoned runs interrupted, preserves terminal rows; continuation creates a new run.
- Real native StateGraph crashes inside a node after a synchronous checkpoint; fresh subprocess restores saved history and explicitly continues it. No custom graph/checkpoint engine.
- Dockerfile copies the added local storage dependency before uv sync.

## Проверки

- Agent: ruff/format/mypy passed; 20 tests passed including five real PostgreSQL cases and fresh-process crash/continue.
- Storage: ruff/format/mypy and 81 tests passed against isolated i3_migrations, including historical report access after removal.
- Migrations: ruff/format/mypy and 19 tests passed, including fresh upgrade/downgrade, cross-database roles and preserving framework tables across run revision rollback.
- `alembic check` against i3_agent with real saver tables: no drift.
- `git diff --check` passed. All requested local checks complete; Docker build remains unavailable.

## Решения и допущения

- Root approved enforced single-worker-per-database recovery. V01 RPC transport remains explicitly in-memory; durable streaming projection/inbox belongs to AG-04, not this spike.
- Runtime login i3_agent_runtime only inherits counterparty_agent; CREATE TABLE is denied. Proof uses i3_agent; destructive storage/migration tests use i3_migrations and i3_migrations_roles. Demo database untouched.
- Full base downgrade refuses unmanaged framework tables by RESTRICT; checkpoint decommission is an explicit deployment action, not an automatic cascade.
- Docker unavailable: COPY reviewed, image build not run.
- Primary sources: https://docs.langchain.com/oss/python/langgraph/persistence and pinned installed AsyncPostgresSaver source.

## Следующее действие

Commit storage/migrations, then service persistence/proof; deliver root deploy/proof commands for integration review.
