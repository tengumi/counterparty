# I5 — Independent API and agent verification

- Status: done — pass with limitations
- Branch: agent/i5-api-agent-verification
- Worktree: .worktrees/i5-api-agent-verification
- Owner scope: docs/checkpoints/tasks/I5.md, tests/integration/i5_*, README backend runbook only
- Depends on: baseline4dee1b2, I7 fix6377532, period correctionb027e44
- Verified source: 953c9b4a12667c374aa2610b744dc133bf10a43f (both fixes merged)
- Last commit: 46bb43a (I7 replay and financial-period regression probe)

## Готово

- Read I1/I3 checkpoints, Specs08 V04 and Specs10 report/REST/RPC contracts.
- Backend README runbook committed atc0c8a99 and integrated by root. Further
  README ownership transferred to I6; I5 makes no additional README edits.

## Текущее состояние

- Independent API/domain/storage/agent verification is complete. Both reproduced
  defects were fixed by their owners and passed replay. Verdict: pass with the
  delivery limitations below; runtime graph/RPC persistence remains AG-04.

## Проверки

- Fresh frozen installs, ruff check/format-check/mypy: passed in all six projects.
- Existing tests, no skips: contracts140 +domain119 +storage83 +API60 +migrations19
  +agent20 =441. Includes real subprocess crash/continue, roles and migration rollback.
- `alembic check` against migrated i5_agent with real saver tables: no drift.
- Independent `i5_checkpoint_fencing_probe.py`: FAIL on 4dee1b2. Killing precisely
  the owner-lock connection lets a replacement mark the run interrupted; old
  run repository rejects access, but the already-open saver/graph persists value42.
  Output: stale_write_succeeded=true, recovered_run_status=interrupted,
  old_owner_rejected_run_write=true. Exit1. Root informed for author fix.
- The same original graph assertion PASSES on I7 fix6377532 after changing only
  `postgres_checkpointer(runtime)` to `postgres_checkpointer(owner)`. Extended
  independent replay: old graph, native aput/aput_writes/adelete_thread all reject
  after replacement; original CheckpointTuple is identical. A separate Python
  process reads committed value1 and continues to6; stale worker writes nothing.
- I7 affected rerun: agent22/storage83/migrations19 tests passed, zero skips;
  local ruff/format/mypy passed in all three. Actual framework-table roundtrip
  repeated after I7 preserved hashes of10/23/13/25 rows and reports no drift.
- Independent `i5_report_http_probe.py`: 17 real TCP/uvicorn REST checks passed
  under i5_web_runtime (counterparty_ui_api), including canonical/forged refs,
  project/tenant/colleague isolation, exact registration timestamp, pinned identity,
  historical removal and deleted-fixture access. Own listener was shut down.
- Approved mock imported directly into i5_api:100 imported/0 failed; no source copy.
- API project DELETE is absent (405), a pre-existing deferred endpoint; deleted
  evidence access was checked using a soft-deleted fixture in the isolated DB.
- The targeted HTTP financial probe reproduced I4's period gap beforeb027e44.
  After the author's correction, all21 HTTP checks pass: the real corpus's
  oldest stocks fact (2023), absent from overview, and its whole FinancialPeriod
  retain period2023; Decimal/raw value, availability and pinned identity match;
  the same evidence remains equal after company removal. Domain122 tests and
  ruff/format/mypy pass; the author's domain regression includes stocks2021.
- Independent real framework-table rollback/upgrade:0005→0004→head retained exact
  hashes of checkpoint_migrations10/checkpoints10/blobs6/writes11 rows; `alembic check`
  still reports no drift. This complements the existing dummy-table migration test.
- I6 independently covers cross-service parity; no duplicate I5 MCP test claim.
- MCP author's own tests do not count as independent I5 evidence.

## Решения и допущения

- DBs: i5_api, i5_storage, i5_migrations, i5_roles, i5_agent on native PG :55435.
- Demo corpus is read-only. Docker unavailable; no installation/build attempted.
- Production fixes remain with their authors.
- Counts are explicit reruns: baseline441, then affected I7 suites22/83/19 and
  domain122 plus independent probes. The unchanged entire baseline was not
  rerun after bounded fixes; root requested targeted acceptance.
- Agent RPC remains the V01 in-memory transport until AG-04. Project DELETE
  remains405; its future cleanup flow is not accepted by these probes.
- I7's protected native saver `_cursor` hook depends on the pinned upstream
  package version. This verifies the current primitive, not a completed agent UX.

## Следующее действие

Root merges the final I5 checkpoint/probes; no implementation follow-up remains
within I5. Retain the worktree and isolated verification databases for review.
