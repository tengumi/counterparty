# I6 — independent MCP / live WEB-08 verification

- Status: verification
- Branch: agent/i6-mcp-web-verification
- Worktree: .worktrees/i6-mcp-web-verification
- Owner scope: docs/checkpoints/tasks/I6.md, apps/web/qa/**, artifacts/qa/WEB-08/**; verification scripts only
- Depends on: MCP I2 integrated in 4dee1b2; I4 final UI integrated in 1896fd8
- Last commit: ba7f22c MCP evidence; WEB-08 harness commit follows

## Готово

- Independent MCP review on source 4dee1b2 passes for MCP-01/02: auth fails closed, actual reader role excludes workspace/DML, native transport, pinned latest, scoped cursor/filters, byte budget including fact-only pages, timeout/cancel/shutdown cleanup.
- Read-only parity script compares the actual UI loader and MCP projections: 100 exact overview DTOs, 96 section pages; all pass.

## Текущее состояние

- Independent verifier; runtime findings go to implementer. I3 work is not independent evidence.
- Live WEB-08 runner uses standard qa/browser.ts, no interception: desktop1440/mobile390 full flow, tablet1024 panel bounds, scoped refs/year/pins and persisted draft/context.
- Browser remains gated on root final integrated signal.

## Проверки

- apps/web on1896fd8: lint/typecheck/92 tests/build/qa:check passed. Existing bundle-size warning. No browser yet.
- services/mcp: frozen sync; ruff check; ruff format --check; mypy — passed.
- MCP_TEST_ADMIN_DATABASE_URL=postgresql+psycopg://g5_admin@127.0.0.1:55435/i6_mcp MCP_CORPUS_DATABASE_URL=postgresql+psycopg://i2_mcp_reader_test@127.0.0.1:55435/g5_demo_utf8 uv run pytest -q -s — 31 passed, no skips (21.84 s). Native isolated synthetic DB, read-only corpus sweep: 5 snapshots × 17 sections, 5555 refs resolved.
- Same corpus env + services/mcp/.venv/bin/python scripts/verification/i6_mcp_parity.py — passed: 100 overviews, 96 section pages, exact shared DTO parity. Script ruff/format passed.
- Docker build unavailable; Docker absent. Third comparison tool/LangChain agent adapter remain outside MCP-01/02; full V05 is not certified.

## Решения и допущения

1. Review MCP auth/role, pinned snapshot, pagination/filter/budget, cleanup and shared parity; reproduce local checks against isolated DB.
2. Prepare live browser scenario and coordinate I4 labels/DTO without runtime edits.
3. After root integration signal, run web checks and single final browser capture at desktop/mobile sizes; report pass/limitations/source SHA/manifest.

## Следующее действие

I7 handed off separately; I6 resumed on 1896fd8. Finish live WEB-08 runner checks, commit clean harness, send final command to root; await integrated capture signal. No browser has been started.
