# I7 — fence native checkpoint writes to live run owner

- Status: done
- Branch: agent/i7-checkpoint-write-fencing
- Worktree: .worktrees/i7-checkpoint-write-fencing
- Owner scope: services/agent/**; storage AgentRunOwner and focused tests; checkpoint I7
- Depends on: baseline 62c9c88; I5 failing probe 6798d00
- Last commit: 1360db1 discovery; implementation commit follows

## Готово

- Native AsyncPostgresSaver now uses the exact physical psycopg connection holding the worker advisory lock; no independent saver connection or pool.
- Shared owner transaction lease serializes repository and native cursor operations. Lock check, native SQL and commit/rollback use the same connection; loss cannot be followed by reconnect.
- Only native `_cursor` lifecycle is wrapped; all saver SQL, pipeline, serialization, graph and deployment setup stay native.
- Runtime `postgres_checkpointer(owner)` requires the live owner. App lifespan and fresh-process worker use this API; deploy remains separate and worker startup performs no DDL.
- Already-open graph and direct aput/aput_writes/adelete_thread all reject after backend termination or explicit owner-lock loss. Replacement recovers interrupted run, retains the exact previous tuple, writes a new graph checkpoint and deletes it normally.

## Текущее состояние

- Implementation ready for I5 independent replay. Adapt probe only from postgres_checkpointer(runtime) to postgres_checkpointer(owner); preserve stale-write assertions.
- I6 paused at ba7f22c, browser never started. Resume only on root task/signal.

## Проверки

- services/agent: frozen sync, ruff check, ruff format --check, mypy — passed.
- `AGENT_TEST_POSTGRES_DSN=postgresql://g5_admin@127.0.0.1:55435/i7_agent AGENT_TEST_RUNTIME_DSN=postgresql://i7_agent_runtime@127.0.0.1:55435/i7_agent uv run pytest -q` — 22 passed, no skips; includes fresh-process native crash/restart and two lost-owner mutation regressions with timeout15s.
- packages/storage: frozen sync, ruff check, ruff format --check, mypy — passed.
- `COUNTERPARTY_TEST_DATABASE_URL=postgresql+psycopg://g5_admin@127.0.0.1:55435/i7_storage uv run pytest -q` — 83 passed, no skips.
- i7_agent migrated to head in isolated PostgreSQL55435; native setup grants runtime role. i7_storage disposable; demo untouched.
- Docker unavailable; no Dockerfile/dependency/migration changes.

## Решения и допущения

- Dependency on protected native `_cursor` hook is explicit and version-sensitive: uv.lock pins langgraph-checkpoint-postgres3.1.2. Repeat native mutation/restart regressions before upgrading.
- One owner per database remains the enforced F0-07 limit. All repository/native cursor work serializes on its dedicated connection.
- V01 transport registry remains in-memory; this fix does not claim durable AG-04 transport.

## Следующее действие

Root/I5: replay 6798d00 probe against this implementation with owner factory argument, then integrate and return this agent to paused I6.
