# I7 — fence native checkpoint writes to live run owner

- Status: discovery
- Branch: agent/i7-checkpoint-write-fencing
- Worktree: .worktrees/i7-checkpoint-write-fencing
- Owner scope: services/agent/**; storage AgentRunOwner and focused tests; checkpoint I7
- Depends on: baseline 62c9c88; I5 failing probe 6798d00
- Last commit: none

## Готово

- I5 defect confirmed by code: run writes use owner connection, saver opens an independent connection and outlives owner loss.
- Proposed boundary: same physical connection and serialized owner transaction for every native saver cursor. Native SQL, pipeline, serialization and graph remain unchanged.

## Текущее состояние

1. Expose owner transaction lease; bind native saver to its physical psycopg connection.
2. Require owner in runtime saver factory; delegate native cursor operations within the lease.
3. Reproduce stale saver rejection after backend termination and lock release, plus native crash/restart, scope, no-DDL and lifecycle checks.
4. Run affected suites, commit, return to I5 for independent probe and resume I6 later.

## Проверки

- Not yet run. Isolated native PostgreSQL i7_agent planned; no demo writes.

## Решения и допущения

- One worker per database remains enforced. No checkpoint engine/codec; thin pinned native saver cursor lifecycle adapter only.
- Checkpointer factory changes from DSN to AgentRunOwner. Deployment setup remains separate from worker startup.
- I6 is paused at ba7f22c; no browser started.

## Следующее действие

Implement the shared owner connection lease and native saver adapter, then run the lost-owner regression first.
