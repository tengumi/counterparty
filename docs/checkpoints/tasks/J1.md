# J1 — Deep Agents harness, контекст и MCP-инструменты

- Status: implementation
- Branch: agent/j1-agent-harness
- Worktree: .worktrees/j1-agent-harness
- Owner scope: services/agent/src/**, services/agent/tests/**, services/agent/pyproject.toml + uv.lock
- Depends on: F0-06 transport, F0-07 checkpointing, MCP-01/02 (services/mcp, read-only)
- Last commit: none

## Готово

- (пока ничего)

## Текущее состояние

- Discovery завершён: deepagents 0.7.13, langchain 1.4, langchain-mcp-adapters 0.3.2 резолвятся под Python 3.13.
- Штатные механизмы подтверждены: `create_deep_agent(model, tools, permissions, backend, checkpointer)`,
  `FilesystemPermission` (first-match-wins), `MultiServerMCPClient.get_tools()`.

## Проверки

- `uv sync --frozen` — passed (baseline окружение сервиса).

## Решения и допущения

- Свой agent loop/роутер инструментов не пишем: harness = `create_deep_agent`, состояние = LangGraph, модель = LangChain adapter.
- Default provider — детерминированный LangChain-адаптер, чтобы сборка и тесты не требовали реальной модели.
- Enforcement filesystem: StateBackend (виртуальные файлы в graph state) + scoped `FilesystemPermission` allow/deny, без `execute`.

## Следующее действие

Добавить зависимости deepagents/langchain/langchain-mcp-adapters в services/agent/pyproject.toml и зафиксировать uv.lock.
