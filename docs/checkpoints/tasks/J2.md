# J2 — MCP compare_companies и полный compose-контур

- Status: implementation
- Branch: agent/j2-mcp-compare-and-compose
- Worktree: .worktrees/j2-mcp-compare-and-compose
- Owner scope: services/mcp/**, compose.yaml, все Dockerfile, deploy/proxy, раздел локального запуска README.md
- Depends on: baseline 125a6a4 (API-05, D-04 приняты)
- Last commit: 2697bf8

## Готово

- MCP-03: tool `compare_companies` поверх reports-only adapter и общей
  `build_comparison_rows` из packages/domain. UI API не импортируется.
  Ранжирования/score нет; отсутствующий отчёт — названный пропуск, не строка.
- OPS-01 (сборка): все Python-образы строятся из корня репозитория,
  venv сразу по /app/.venv, локальные path-пакеты переустанавливаются по имени.
- OPS-01 (compose): postgres+volume, migrate, roles (4 login), import,
  checkpoints (DDL job), ui_api, agent, mcp, web, reverse proxy; healthcheck у
  каждого сервиса.

## Текущее состояние

- `services/mcp`: ruff/mypy/pytest зелёные (32 passed, 8 skipped — DB и live parity).
- Образы собираются; полный `docker compose build` ещё выполняется.

## Проверки

- `cd services/mcp && uv run ruff check . && uv run ruff format --check . && uv run mypy && uv run pytest` — passed
- `docker compose config` — passed
- `docker compose build` / `up` — в процессе

## Решения и допущения

- Локальные path-пакеты: non-editable + явный `--reinstall-package` для
  counterparty-contracts/domain/storage во всех sync-шагах (Dockerfile и compose).
- Единый edge на :5173 (`deploy/proxy/nginx.conf`): browser видит один origin,
  cookie остаётся first-party, SPA собирается без API base URL и без секретов.
- MCP получает только SHA256 digest; plaintext-токен ни в один сервис не передаётся.
- Parity доказывается live-тестом `services/mcp/tests/test_parity.py` по HTTP,
  без импорта UI API.

## Следующее действие

Дождаться сборки, поднять контур, снять healthchecks и выполнить live parity.
