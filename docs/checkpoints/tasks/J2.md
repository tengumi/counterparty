# J2 — MCP compare_companies и полный compose-контур

- Status: discovery
- Branch: agent/j2-mcp-compare-and-compose
- Worktree: .worktrees/j2-mcp-compare-and-compose
- Owner scope: services/mcp/**, compose.yaml, все Dockerfile, раздел локального запуска README.md
- Depends on: baseline 125a6a4 (API-05, D-04 приняты)
- Last commit: none

## Готово

- Discovery: контракты `CompareCompaniesInput`, `Comparison`, `ComparisonEnvelope`
  уже существуют; `build_comparison_rows` в packages/domain используется UI API
  (`services/ui_api/src/counterparty_ui_api/reports.py`). MCP переиспользует ту же
  чистую функцию поверх reports-only adapter — parity структурная, доказывается тестом.

## Текущее состояние

- MCP отдаёт только overview и section.
- compose.yaml поднимает postgres, migrate, roles, import.

## Проверки

- не запускались

## Решения и допущения

- Локальные path-пакеты: non-editable + явный `--reinstall-package` для
  counterparty-contracts/domain/storage во всех sync-шагах (Dockerfile и compose).

## Следующее действие

Реализовать compare в reader/database/runtime/app MCP.
