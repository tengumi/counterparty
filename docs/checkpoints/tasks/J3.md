# J3 — WEB-09 транспорт агента и WEB-11 решение пользователя

- Status: discovery
- Branch: agent/j3-web-transport-decision
- Worktree: .worktrees/j3-web-transport-decision
- Owner scope: `apps/web/**`
- Depends on: baseline 125a6a4; публичный контракт `services/agent/src/counterparty_agent/transport/**` (read-only)
- Last commit: none

## Готово

- Discovery: транспорт F0-06 (`apps/web/src/chat/**`) уже существует, но смонтирован
  только в `fixtureMode && project.isDemo`; живой путь показывает заглушку
  «Помощник пока недоступен».
- Найдено: `@assistant-ui/react` 0.15.18 штатно поддерживает `resumeApi` и
  `thread.resumeRun()` — reconnect строится на них, свой codec не нужен.
- Найдено: UI API не реализует `GET /projects/{p}/threads/{t}/conversation`,
  `GET/POST /projects/{p}/decisions` и `GET /projects/{p}/artifacts`
  (Specs 10 §5). `services/**` вне ownership J3.

## Текущее состояние

- Изменений в коде нет.

## Проверки

- `npm ci` — passed (apps/web).

## Решения и допущения

- WEB-11 строится против принятого REST-контракта Specs 10 §5; пока сервер его не
  отдаёт, UI честно деградирует и НЕ сохраняет решение локально.
- Обрыв сети отделяется от завершения/отмены run: если run не терминален,
  показывается «связь потеряна» + переподключение, а не «помощник ответил».

## Следующее действие

Смонтировать живой транспорт на production-пути `ChatSurface` и покрыть тестами
честную деградацию при недоступном Agent Service.
