[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "counterparty-import-reports"
version = "0.1.0"
description = "Extended JSON decoder and schema fingerprint for the approved report snapshot"
requires-python = ">=3.13,<3.14"
dependencies = ["counterparty-storage"]

[dependency-groups]
dev = [
  "mypy>=1.18,<2",
  "pytest>=8.4,<10",
  "ruff>=0.13,<0.16",
]

[tool.uv.sources]
counterparty-storage = { path = "../../packages/storage", editable = true }

[tool.hatch.build.targets.wheel]
packages = ["src/import_reports"]

[tool.ruff]
target-version = "py313"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "RUF", "D"]

[tool.ruff.lint.pydocstyle]
convention = "google"

[tool.mypy]
python_version = "3.13"
strict = true
files = ["src", "tests"]

[tool.pytest.ini_options]
testpaths = ["tests"]
